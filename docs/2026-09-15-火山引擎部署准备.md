# 故事旧货铺 · 火山引擎部署记录

> 当前状态：已于 2026-09-15 发布并完成公网验收，线上 revision 2。用户已确认新增资源费用。

**作品链接：[点击开始游玩](https://sda570na48o4l1bu14h9m.apigateway-cn-beijing.volceapi.com/)**

## 已核实的资源

- veFaaS CLI 0.3.1，官方技能包为最新版本，已有本地加密凭据有效。
- 北京地域网关 `qingchen-gw` 可复用。
- `weilan-frontend`、`weilan-backend` 是其他项目，未修改。
- 已有私有 TOS 桶可用；游戏只使用独立 `story-shop-v1/` 前缀。
- 云端正式存档前缀：`story-shop-v1/production/`；验证数据放在 `story-shop-v1/qa/`。

## 本次实现

| 部分 | 变更 |
| --- | --- |
| 模型 | Gemini 3.8 Flash，经用户现有服务商的 Gemini 原生接口调用 |
| 速度 | minimal 思考级别、连接复用、预置提问直接返回；保留独立剧情审校 |
| 返回格式 | 结构化返回并验证字段；兼容完整外层 JSON 代码块；截断、非法数据不提交进度 |
| 云存档 | `STORE_BACKEND=tos`，私有对象保存会话、收藏与幂等响应；ETag 条件写防止并发覆盖 |
| 单一网址 | `app.web:app` 同时提供网页和 API；前端默认同源 |
| 资源加载 | 图片、音频设置缓存，带内容哈希的 JS/CSS 长期缓存，首页和私有 API 禁止缓存 |
| 密钥保护 | 白名单暂存部署目录，扫描实际包中不含模型与存储密钥；环境配置单独导入 |

## 验证结果

- 后端 55 项测试、前端 21 项测试通过，正式构建通过。
- 真实 TOS 条件写测试：过期 ETag 写入被拒，返回 412，当前数据不被覆盖。
- 真实 TOS 完整首章：51 段剧情推进、收藏成功；更换应用实例后恢复，重新体验回到 FILM001，收藏内容不变。
- Gemini 真实测试：正常物件问题、未公开事实、未交代病名、故事总结、无关问题和混合任务。
- 本机小样本：自由问答 4.05–7.48 秒；无关问题 1.14–2.40 秒；预置提问无需调用模型。公网实测无关问题 3.30 秒；浏览器真实物件问答已通过。实际延迟随问题、网关和模型负载变化。

- 公网完整首章：51 段正常计时推进、收藏成功、重试不重复收藏；期间发布 revision 2，继续读取成功。
- 浏览器：故事选择显示「重新体验」；从开场恢复，收藏数仍为 01，相机图案与详情可打开；点击「下一句」正常推进。
- 公网 87 个资源逐一校验 SHA256，包括 71 条固定配音、背景音乐及图片；看山和旁白音频实际进入播放状态。
- 桌面 1460 px、手机首页 390 px 宽度无横向溢出。未进行大规模压力测试。

## 发布参数

- 应用名：`gushijiuhuopu`，应用 ID：`a03e351357f3`。
- 函数 ID：`ujjqwbbx`，线上 revision：`2`，北京地域。
- 网关：`qingchen-gw`。
- 规格：0.5 vCPU / 1 GiB；已设置最小 1、最大 2 个实例。
- 启动：`python -m uvicorn app.web:app --host 0.0.0.0 --port 8000`。
- 正式配置：`AI_MODE=custom`、`AI_PROVIDER_FACTORY=app.story_ai:create_provider`、`AI_MODEL=gemini-3.8-flash`、`AI_API_PROTOCOL=gemini`、`STORY_PATH=stories/lost-sunshine.json`、`STORE_BACKEND=tos`。
- `TOS_ACCESS_KEY_ID`、`TOS_SECRET_ACCESS_KEY`、`AI_API_KEY` 等仅通过受控环境配置导入。
- `TOS_ENDPOINT` 云端采用现有北京内网端点，本地验证使用公网端点。
- `.local-preview/volc-deploy/` 是独立的白名单部署目录；不要直接上传含 `.env.ai` 的整个工作目录。
- `.local-preview/volc-production.env` 为受保护的未跟踪环境文件，不复制到部署目录。

## 数据和配音的边界

- 本地仍默认 SQLite；云端 TOS 模式不依赖 `/tmp`，无需靠常驻实例保住存档。
- 每个匿名浏览器会话持有独立 bearer token。对象标识不能作为 token 使用。清除浏览器存储或换设备不会自动恢复原会话。
- 会话闲置 30 天后拒绝访问；云对象保留作恢复副本，需在维护窗口归档清理。当前最大会话数 1000；容量包含保留的过期对象。并发创建时数量限制是尽力而为，需监控容量。
- AI 每日尝试上限 200，跨实例共享。不是人民币费用上限。
- 固定的剧情和看山配音随版本打包，服务重建后仍在。实时配音依赖原本的 Apple Silicon 本机服务，云端尚无等效服务；生产配置暂留空，动态问题以文字呈现，不影响固定配音。
- 验证前缀有少量测试对象。当前存储凭据未获删除对象权限，因此保留测试对象；没有为清理而扩大权限。

## 费用确认与后续步骤

官方计费文档：https://www.volcengine.com/docs/6662/1269135 （页面更新时间 2025-08-27）。

一个 0.5 vCPU / 1 GiB 预留实例，按文档国内单价计算约 2.63 元/天，30 天约 79 元；另外计算网关流量、对象存储和模型调用费用。最终以控制台为准，不是总费用上限。

用户已明确回复「确认」，随后创建应用并发布。其他项目未修改。

## 后续更新与恢复

1. 从最新 `main` 建立功能分支完成修改，运行后端测试、前端测试。前端正式构建必须使用下方命令；普通 `npm run build` 默认采用模拟模式，不能直接用于此次生产发布。

   ```bash
   cd frontend
   VITE_TRANSPORT=http VITE_API_BASE_URL='' npm run build
   cd ..
   ```

2. 使用本机 `.local-preview/prepare_volc_release.py` 更新白名单发布目录；确保不将密钥或本地数据库复制进去。换电脑时按上述白名单重新建立暂存目录，并从安全渠道取得环境配置。
3. 在 `.local-preview/volc-deploy/` 核对绑定的是应用 `a03e351357f3`、函数 `ujjqwbbx`，再运行 `vefaas deploy --skipbuild --yes`。依赖会在云端安装。
4. 只有环境变量改变才单独导入受控环境文件；禁止把导出的明文配置提交到 GitHub 或粘贴在公开交接里。
5. 发布后检查首页、健康接口、AI 提问、音频、刷新恢复和收藏。`vefaas app revisions --id a03e351357f3 -o table` 可查看版本记录。
6. 若后续版本出错，在火山控制台核对函数与目标版本后回滚到本次已验收的 revision 2。云存档独立于代码版本；含数据结构变更时先备份并确认兼容性。

当前共享网关复用 `qingchen-gw`，不要删除或重建该网关。旧阿里云域名部署方案属于历史备选，本次对外提交使用本文顶部火山引擎地址。

## 团队接手

- GitHub：[PR #6](https://github.com/chenjieliefu/gushijiuhuopu/pull/6)，已合并到 `main`，合并提交 `13a7452`。队员直接拉取主分支即可。
- 云端固定配音已上线；动态 AI 语音需要后续接入适合云端的语音服务，目前新回答显示文字。
- 每日 AI 尝试上限 200，比赛集中体验前按实际规模评估；达到上限会停止当日新增模型调用。
- 本次上线验收记录保存在本机 `.local-preview/production-qa-result.json`；会话凭据等私有测试文件不进入仓库。

## 最终同步核对（2026-09-15）

- 应用运行代码对应 Git 提交 `8bd43f7`，该版本现已合并到远端 `main`，线上 revision 2。
- 从云端下载代码核对：21 个后端、剧情与依赖文件和本地一致。
- 按正式参数重新构建：254 个前端文件与云端发布包逐字节一致；公网逐文件 SHA256 也全部一致。
- PR #6 已成功合并到 `main`（合并提交 `13a7452`）；合并前后文件内容一致，主分支现已包含已上线代码。
- 本次核对仅补充交接文档，无游戏代码变更，因此无需重复发布。最终交接入口见 [最终交接总结](2026-09-15-最终交接总结.md)。
